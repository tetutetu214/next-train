# 監査とロードマップ（2026-07-07 / Fable 5 作成）

以降のセッションはこの優先順で実行する。状況が変わっていたら premise-check してから従うこと。

## 現状スナップショット（前提修正あり）

- メモリの「モックで MVP 完成・ODPT 実データ待ち」は**大きく古い**。実際は 2026-06-10 に ODPT API キー投入済みで、**7事業者440駅の実データ本番運用中**（Workers Static Assets 単一 Worker 配信）。過去 PR 5件すべてマージ済み、open PR なし。
- 現在ブランチはスプリットフラップ UI の実験（feature/split-flap-display）。「見た目の変化が乏しく使えない」判断で PR 未作成のまま保留。
- テスト7ファイル(計1,139行)あり。ただし CI（GitHub Actions）未設定で、担保は deploy.sh 内のローカル実行のみ。

## 監査所見（リスク順）

1. **[中] マージ判断が手動テストに依存**: CI がなく、テストは deploy.sh のデプロイ前フローだけ。壊れた main に気づく仕組みが薄い。Actions はアカウント再フラグ履歴（reference_github_actions_account_flag）があるため、導入時はローカル fallback を残す。
2. **[中] JR東チャレンジ2025 ライセンスの継続確認が 6/11 で停止**: データ提供条件は期限つき。todo.md:40 のまま放置すると、失効に気づかず配信が静かに壊れる。
3. **[低] スプリットフラップ実験ブランチの宙吊り**: 継続（全桁シャッフル着地の再強化）か破棄かの意思決定待ち。コード自体は main に影響なし。
4. **[低] wrangler v3.86 のまま**: v4 移行推奨警告あり。急がないが計画に載せる。
5. **[低] docs の重複**: todo.md の古い未完了項目（骨格モック実装）は完了済みと重複。

## ロードマップ（この順で実行）

### Step 1: 衛生タスク（chore、即実行可）
- todo.md の重複項目掃除、マージ済みリモートブランチ3本（multi-operator / train-info-banner / odpt-sameas-id）の削除
- メモリ project_next_train を実データ運用の実態に更新

### Step 2: スプリットフラップの決着（てつてつの判断）
- 「全桁シャッフル着地」を1回だけ試して判定するか、ブランチを閉じるか。宙吊りが一番高くつく

### Step 3: 運用の下支え（小PR）
- JR東ライセンス継続の実機確認 → knowledge.md に記録
- CI 導入判断（Actions + ローカル fallback。またはローカル pre-push hook で妥協）

### Step 4: PWA 化（todo.md:29、機能追加はこれが本命）
- Service Worker + Manifest。ホーム画面追加で「駅で開く」体験が本命化する

## 担当分業の目安

- Step 1: サブエージェントで十分 / Step 3-4 実装: Codex または implementer / 判断: てつてつ
